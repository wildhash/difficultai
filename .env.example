# ============================================================================
# DifficultAI LiveKit Agent - Environment Configuration
# ============================================================================
# 
# This file defines ALL required environment variables for the agent.
# Copy this to .env and fill in your values.
#
# REQUIRED VARIABLES:
#   OPENAI_API_KEY, LIVEKIT_URL, LIVEKIT_API_KEY, LIVEKIT_API_SECRET
#
# CONFIGURATION LOCATIONS:
#   - LOCAL DEVELOPMENT: Set all variables in .env file (this directory)
#   - LIVEKIT CLOUD DEPLOYMENT: Set via LiveKit Cloud dashboard > Agent Settings > Environment Variables
#   - DOCKER DEPLOYMENT: Pass via --env-file .env or docker-compose.yml environment section
#
# ============================================================================

# ----------------------------------------------------------------------------
# OpenAI Configuration (REQUIRED)
# ----------------------------------------------------------------------------
# Where to set:
#   - Local: .env file
#   - LiveKit Cloud: Dashboard > Environment Variables
#   - Docker: --env-file or docker-compose
#
# Get your key from: https://platform.openai.com/api-keys
# Note: Requires access to OpenAI Realtime API
OPENAI_API_KEY=your_api_key_here

# ----------------------------------------------------------------------------
# LiveKit Configuration (REQUIRED for voice agent)
# ----------------------------------------------------------------------------
# Where to set:
#   - Local: .env file
#   - LiveKit Cloud: Auto-provided by platform (don't set manually)
#   - Docker: --env-file or docker-compose
#
# For LiveKit Cloud:
#   Get credentials from: https://cloud.livekit.io > Project Settings
#   LIVEKIT_URL format: wss://your-project.livekit.cloud
#
# For Self-Hosted:
#   Use your LiveKit server URL (e.g., wss://livekit.yourdomain.com)
LIVEKIT_URL=wss://your-livekit-server.com
LIVEKIT_API_KEY=your_livekit_api_key
LIVEKIT_API_SECRET=your_livekit_api_secret

# ----------------------------------------------------------------------------
# Optional: Deepgram Configuration
# ----------------------------------------------------------------------------
# Where to set:
#   - Local: .env file
#   - LiveKit Cloud: Dashboard > Environment Variables
#   - Docker: --env-file or docker-compose
#
# Used for STT fallback (optional, not needed for Realtime API)
# Get your key from: https://console.deepgram.com/
DEEPGRAM_API_KEY=your_deepgram_api_key

# ----------------------------------------------------------------------------
# Voice Configuration
# ----------------------------------------------------------------------------
# Default voice for OpenAI Realtime API
# Options: alloy, echo, fable, onyx, nova, shimmer, marin
# Where to set: Local .env, LiveKit Cloud dashboard, or Docker env
DEFAULT_VOICE=marin

